# Tasks

`current.md` is the lightweight shared task board. Keep one row per deliverable and update it when ownership or status changes.

Allowed statuses: `backlog`, `analysis`, `design`, `ready`, `in_progress`, `qa`, `review`, `blocked`, `done`.

